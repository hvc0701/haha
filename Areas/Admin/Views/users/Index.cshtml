@model IEnumerable<Duc_BTL.Areas.Admin.Models.user>

@{
    ViewData["Title"] = "Danh sách người dùng";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="row">
  <div class="col-lg-9 mb-4">
    <!-- Simple Tables -->
    <div class="card">
      <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
        <h6 class="m-0 font-weight-bold text-primary">Danh sách người dùng</h6>
      </div>
      <div class="table-responsive">
        <table class="table align-items-center table-flush">
          <thead class="thead-light">
            <tr>
              <th>ID</th>
              <th>Name</th>
              <th>Email</th>
              <th>Role</th>
              <th>#</th>
            </tr>
          </thead>
          <tbody>
            @foreach (var item in Model)
            {
              <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Email</td>
                <td>@item.Role</td>
                <td>
                  <a href="/admin/users/edit?id=@item.Id" class="btn btn-outline-primary">Sửa</a>
                  <a href="/admin/users/details?id=@item.Id" class="btn btn-outline-info">Chi tiết</a>
                  <a href="/admin/users/delete?id=@item.Id" class="btn btn-outline-danger">Xóa</a>
                </td>
              </tr>
            }
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
<a href="/admin/users/create" class="btn btn-outline-primary mb-5">Thêm mới</a>